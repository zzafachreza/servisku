import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function Transaksi() {
  return (
    <View
      style={{
        padding: 10,
      }}>
      <Text>Dalam Proses Pengerjaan</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
